import NoteIcon from '@material-ui/icons/Note';

let Header = () => {
    return(
        <header>
        
            <h1><NoteIcon /> Keep Notes</h1>
        </header>
    )
}

export default Header